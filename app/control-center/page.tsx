import AdminShell from '@/components/AdminShell';import StatCard from '@/components/StatCard';import { one } from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Control(){const s=await one('select (select count(*) from users) users,(select count(*) from questions) questions,(select count(*) from attempts) attempts');return <AdminShell><h1 className="text-4xl font-black mb-8">Admin Control Center</h1><div className="grid md:grid-cols-3 gap-5"><StatCard label="Users" value={s?.users||0}/><StatCard label="Questions" value={s?.questions||0}/><StatCard label="Attempts" value={s?.attempts||0}/></div><div className="glass rounded-3xl p-6 mt-8"><h2 className="text-2xl font-bold">Hidden Admin Panel</h2><p className="text-slate-400 mt-2">This route is not linked on student pages. Use role protection before production.</p></div></AdminShell>}
